# Бот-викторина о двигателях паровозов

Telegram-бот на aiogram с викториной о двигателях паровозов и интеграцией YandexGPT.

## Функциональность

- 🎯 Викторина из 5 вопросов о двигателях паровозов
- ✅ Каждый вопрос имеет 3 варианта ответа
- 🤖 Интеграция с YandexGPT для ответов на вопросы пользователя
- 📊 Подсчет правильных ответов и процент успешности

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram:
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot` и следуйте инструкциям
   - Скопируйте полученный токен

3. Откройте файл `env` и замените `YOUR_BOT_TOKEN_HERE` на ваш токен бота:
```
BOT_TOKEN=ваш_токен_здесь
```

4. Убедитесь, что файл `env` содержит правильные данные для YandexGPT (уже настроен)

## Запуск

### Локальный запуск

```bash
python bot.py
```

### Запуск в Docker

1. Создайте файл `.env` на основе `env.example`:
```bash
# Скопируйте env.example в .env и заполните значения
cp env.example .env
# Затем отредактируйте .env и укажите ваш BOT_TOKEN
```

Или если у вас уже есть файл `env`, скопируйте его:
```bash
cp env .env
```

2. Запустите через docker-compose (рекомендуется):
```bash
docker-compose up -d
```

Или соберите и запустите вручную:
```bash
# Сборка образа
docker build -t steam-train-bot .

# Запуск контейнера с переменными окружения
docker run -d --name steam_train_bot --env-file .env steam-train-bot

# Или передайте переменные напрямую
docker run -d --name steam_train_bot \
  -e BOT_TOKEN="ваш_токен" \
  -e LLM_MODEL_URI="..." \
  steam-train-bot
```

3. Просмотр логов:
```bash
docker-compose logs -f
# или
docker logs -f steam_train_bot
```

4. Остановка:
```bash
docker-compose down
# или
docker stop steam_train_bot
```

## Использование

1. Отправьте боту команду `/start`
2. Выберите "Начать викторину" для прохождения викторины
3. Выберите "Задать вопрос YandexGPT" для получения ответа на ваш вопрос о паровозах

## Структура проекта

- `bot.py` - основной файл бота
- `quiz_data.py` - данные викторины (вопросы и ответы)
- `yandex_gpt.py` - модуль для работы с YandexGPT
- `config.py` - конфигурация для YandexGPT
- `env` - файл с переменными окружения (для локального запуска)
- `.env` - файл с переменными окружения (для Docker)
- `requirements.txt` - зависимости проекта
- `Dockerfile` - конфигурация Docker образа
- `docker-compose.yml` - конфигурация для docker-compose
- `.dockerignore` - файлы, исключаемые из Docker образа

